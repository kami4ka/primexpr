{
  "author": "Oleg Kulyk",
  "name": "primexpr",
  "version": "1.0.0",
  "description": "Check prime number using only RegExp!",
  "dependencies": {},
  "devDependencies": {
    "jest-cli": "^22.4.3"
  },
  "scripts": {
    "test": "jest --no-cache --bail --verbose"
  },
  "license": "MIT",
  "main": "index.js",
  "engines": {
    "node": ">=4.0.0"
  },
  "jest": {
    "collectCoverage": true
  }
}
